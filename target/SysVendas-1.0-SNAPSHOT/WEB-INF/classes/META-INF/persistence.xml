<?xml version="1.0" encoding="UTF-8"?>
<persistence version="3.0"
             xmlns="https://jakarta.ee/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_0.xsd">

    <persistence-unit name="SysVendasPU" transaction-type="JTA">
        <jta-data-source>jdbc/sysvendasDS</jta-data-source>

        <!-- Entidades gerenciadas -->
        <class>br.upf.sysvendas.entity.UsuariosEntity</class>

        <!-- Inclui automaticamente outras classes da aplicação -->
        <exclude-unlisted-classes>true</exclude-unlisted-classes>

        <properties>
            <!-- Geração automática das tabelas -->
            <property name="jakarta.persistence.schema-generation.database.action" value="drop-and-create"/>
            <property name="jakarta.persistence.schema-generation.create-source" value="metadata"/>
            <property name="jakarta.persistence.schema-generation.drop-source" value="metadata"/>

            <!-- Dialeto do banco PostgreSQL -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect"/>
        </properties>
    </persistence-unit>

</persistence>
